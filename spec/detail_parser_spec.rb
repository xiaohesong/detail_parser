require "spec_helper"

RSpec.describe DetailParser do
  it "has a version number" do
    expect(DetailParser::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
